# WebSocket server URL for realtime sync across devices.
# When set, the app uses WebSocket instead of BroadcastChannel (same-tab only).
# Development: ws://localhost:8787 (run ws-server: cd ws-server && npm start)
# Production: wss://your-ws-server.example.com (required for HTTPS frontends)
# See DEPLOY.md for deployment instructions.
VITE_WS_URL=
